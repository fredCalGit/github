export type User = {
  avatarUrl: string;
  bio: string;
  company: string;
  createdAt: string;
  email: string;
  id: string;
  location: string;
  name: string;
  login: string;
  repositories: {
    totalCount: number;
  };
};
